apiVersion: v1
kind: ConfigMap
metadata:
  name: go2rtc-gatus-ep
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: go2rtc
        group: guarded
        url: http://go2rtc.default.svc.cluster.local:1984/api
        interval: 1m
        ui:
          hide-hostname: true
          hide-url: true
        conditions:
          - "[STATUS] == 200"
          - "[RESPONSE_TIME] < 5000"
        alerts:
          - type: pushover
