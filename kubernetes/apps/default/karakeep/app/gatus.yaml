apiVersion: v1
kind: ConfigMap
metadata:
  name: karakeep-gatus-ep
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: karakeep
        group: guarded
        url: http://karakeep.default.svc.cluster.local:3000/
        interval: 1m
        ui:
          hide-hostname: true
          hide-url: true
        conditions:
          - "[STATUS] == 200"
          - "[RESPONSE_TIME] < 5000"
        alerts:
          - type: pushover
