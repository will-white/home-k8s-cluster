apiVersion: v1
kind: ConfigMap
metadata:
  name: stirling-pdf-gatus-ep
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: stirling-pdf
        group: guarded
        url: http://stirling-pdf.default.svc.cluster.local:8080/
        interval: 1m
        ui:
          hide-hostname: true
          hide-url: true
        conditions:
          - "[STATUS] == 200"
          - "[RESPONSE_TIME] < 5000"
        alerts:
          - type: pushover
