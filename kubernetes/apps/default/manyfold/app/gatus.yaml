apiVersion: v1
kind: ConfigMap
metadata:
  name: manyfold-gatus-ep
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: manyfold
        group: guarded
        url: http://manyfold.default.svc.cluster.local:3214/
        interval: 1m
        ui:
          hide-hostname: true
          hide-url: true
        conditions:
          - "[STATUS] == 200"
          - "[RESPONSE_TIME] < 5000"
        alerts:
          - type: pushover
