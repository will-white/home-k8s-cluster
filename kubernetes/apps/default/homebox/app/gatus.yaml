apiVersion: v1
kind: ConfigMap
metadata:
  name: homebox-gatus-ep
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: homebox
        group: guarded
        url: http://homebox.default.svc.cluster.local:7745/
        interval: 1m
        ui:
          hide-hostname: true
          hide-url: true
        conditions:
          - "[STATUS] == 200"
          - "[RESPONSE_TIME] < 5000"
        alerts:
          - type: pushover
