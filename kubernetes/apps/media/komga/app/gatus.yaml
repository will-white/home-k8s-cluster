apiVersion: v1
kind: ConfigMap
metadata:
  name: komga-gatus-ep
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: komga
        group: guarded
        url: http://komga.media.svc.cluster.local:25600/actuator/health
        interval: 1m
        ui:
          hide-hostname: true
          hide-url: true
        conditions:
          - "[STATUS] == 200"
          - "[RESPONSE_TIME] < 5000"
        alerts:
          - type: pushover
