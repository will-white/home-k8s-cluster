apiVersion: v1
kind: ConfigMap
metadata:
  name: komf-gatus-ep
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: komf
        group: guarded
        url: http://komf.media.svc.cluster.local:8085/
        interval: 1m
        ui:
          hide-hostname: true
          hide-url: true
        conditions:
          - "[STATUS] == 200"
          - "[RESPONSE_TIME] < 5000"
        alerts:
          - type: pushover
