apiVersion: v1
kind: ConfigMap
metadata:
  name: autobrr-gatus-ep
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: autobrr
        group: guarded
        url: http://autobrr.media.svc.cluster.local:80/api/healthz/liveness
        interval: 1m
        ui:
          hide-hostname: true
          hide-url: true
        conditions:
          - "[STATUS] == 200"
          - "[RESPONSE_TIME] < 5000"
        alerts:
          - type: pushover
