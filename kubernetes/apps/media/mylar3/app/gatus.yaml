apiVersion: v1
kind: ConfigMap
metadata:
  name: mylar3-gatus-ep
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: mylar3
        group: guarded
        url: http://mylar3.media.svc.cluster.local:8090/
        interval: 1m
        ui:
          hide-hostname: true
          hide-url: true
        conditions:
          - "[STATUS] == 200"
          - "[RESPONSE_TIME] < 5000"
        alerts:
          - type: pushover
