apiVersion: v1
kind: ConfigMap
metadata:
  name: suwayomi-gatus-ep
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: suwayomi
        group: guarded
        url: http://suwayomi.media.svc.cluster.local:4567/
        interval: 1m
        ui:
          hide-hostname: true
          hide-url: true
        conditions:
          - "[STATUS] == 200"
          - "[RESPONSE_TIME] < 5000"
        alerts:
          - type: pushover
