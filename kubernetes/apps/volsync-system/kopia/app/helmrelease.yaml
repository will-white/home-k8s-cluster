---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kopia
spec:
  chartRef:
    kind: OCIRepository
    name: kopia
  interval: 1h
  values:
    controllers:
      kopia:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          init-config:
            image:
              repository: busybox
              tag: latest
            command:
              - sh
              - -c
              - cp /config-readonly/repository.config /config/repository.config
          init-repository:
            image:
              repository: ghcr.io/home-operations/kopia
              tag: 0.22.3@sha256:eeebd12fd4b3a9c25b9f711fff32454f62e2d5e2d431ab6806ad21c52f414807
            env:
              KOPIA_S3_DISABLE_TLS: "true"
            envFrom:
              - secretRef:
                  name: kopia-bucket
              - secretRef:
                  name: kopia-password
            command:
              - sh
              - -c
              - |
                kopia repository connect s3 \
                  --bucket=volsync-backups \
                  --endpoint=rook-ceph-rgw-ceph-objectstore.rook-ceph.svc \
                  --access-key=$AWS_ACCESS_KEY_ID \
                  --secret-access-key=$AWS_SECRET_ACCESS_KEY \
                  --disable-tls \
                  --password=$KOPIA_PASSWORD \
                  --config-file=/config/repository.config || \
                kopia repository create s3 \
                  --bucket=volsync-backups \
                  --endpoint=rook-ceph-rgw-ceph-objectstore.rook-ceph.svc \
                  --access-key=$AWS_ACCESS_KEY_ID \
                  --secret-access-key=$AWS_SECRET_ACCESS_KEY \
                  --disable-tls \
                  --password=$KOPIA_PASSWORD \
                  --config-file=/config/repository.config
        containers:
          app:
            image:
              repository: ghcr.io/home-operations/kopia
              tag: 0.22.3@sha256:eeebd12fd4b3a9c25b9f711fff32454f62e2d5e2d431ab6806ad21c52f414807
            env:
              KOPIA_WEB_ENABLED: "true"
              KOPIA_WEB_PORT: &port 51515
              TZ: ${TIMEZONE}
              AWS_S3_ENDPOINT: http://rook-ceph-rgw-ceph-objectstore.rook-ceph.svc
              KOPIA_S3_DISABLE_TLS: "true"
            envFrom:
              - secretRef:
                  name: kopia-bucket
              - secretRef:
                  name: kopia-password
            args:
              - --without-password
            ports:
              - name: http
                containerPort: *port
                protocol: TCP
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: *port
                  initialDelaySeconds: 120
                  periodSeconds: 30
                  timeoutSeconds: 5
                  failureThreshold: 5
              readiness: *probes
              startup:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: *port
                  initialDelaySeconds: 30
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 60
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 1Gi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        ports:
          http:
            port: *port
    ingress:
      app:
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: Storage & Infrastructure
          gethomepage.dev/name: Kopia
          gethomepage.dev/icon: kopia.png
          gethomepage.dev/description: Backup Repository Browser
        className: internal
        hosts:
          - host: "kopia.${SECRET_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
                service:
                  identifier: app
                  port: http
    configMaps:
      config:
        data:
          repository.config: |-
            {
              "storage": {
                "type": "s3",
                "config": {
                  "bucket": "volsync-backups",
                  "endpoint": "rook-ceph-rgw-ceph-objectstore.rook-ceph.svc",
                  "doNotUseTLS": true,
                  "doNotVerifyTLS": true
                }
              },
              "username": "volsync",
              "description": "volsync",
              "enableActions": false
            }
    persistence:
      config-file:
        type: configMap
        identifier: config
        globalMounts:
          - path: /config-readonly/repository.config
            subPath: repository.config
      config:
        type: emptyDir
        advancedMounts:
          kopia:
            init-config:
              - path: /config
            init-repository:
              - path: /config
            app:
              - path: /config
      tmpfs:
        type: emptyDir
        advancedMounts:
          kopia:
            init-repository:
              - path: /config/cache
                subPath: cache
              - path: /config/logs
                subPath: logs
              - path: /tmp
                subPath: tmp
            app:
              - path: /config/cache
                subPath: cache
              - path: /config/logs
                subPath: logs
              - path: /tmp
                subPath: tmp
