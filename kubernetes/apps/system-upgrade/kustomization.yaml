---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./namespace.yaml
  - ./system-upgrade-controller/ks.yaml
  - ./plans/ks.yaml

# Note: Upgrade Plans are generated by GitHub Actions when Renovate updates talconfig.yaml
# Both worker and control plane Plans are automatically applied by Flux after PR merge
# Control plane Plans use 'after:' dependency to wait for workers to complete first
