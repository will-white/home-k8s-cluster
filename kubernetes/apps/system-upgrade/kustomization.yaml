---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./namespace.yaml
  - ./system-upgrade-controller/ks.yaml
  - ./version-checker/ks.yaml
  - ./plans/ks.yaml

# Note: Plan templates in the plans/ directory are examples
# Actual Plans are generated by GitHub Actions when new versions are detected
# Worker Plans (01-*) are automatically applied via Flux after PR merge
# Control Plane Plans (02-*) require manual application after workers complete
